# 2026 数学建模国赛 C 题团队快速上手指南

> 目标：让第一次接触本项目的队友在 10 分钟内知道题目在做什么、文件放在哪里、环境怎么装、接下来做什么。

## 1. 一句话认识这道题

C 题研究一个包含小区负载、光伏发电、储能设备和外部电网的微网系统。我们需要决定每个时间段购买多少电、储能充多少电和放多少电，在始终满足小区用电需求的前提下，尽量降低总购电费用。

整道题可以用一套统一的混合整数线性规划（MILP）和滚动优化框架完成：

```text
问题 1：单日确定性 MILP
问题 2：逐日递推 MILP，加入预测偏差与紧急购电
问题 3：0:00、6:00、12:00、18:00 多阶段滚动 MPC
问题 4：将固定电价替换为波动电价，扩展问题 2 和问题 3
```

四问不是四个互不相关的模型，而是逐步扩展：

```text
基础购电与储能模型
    ↓
加入逐日变化、预测误差和紧急购电
    ↓
加入多阶段预测更新和购电调整
    ↓
加入实时波动电价
```

## 2. 四个问题分别要做什么

### 问题 1：单日确定性优化

- 已知一天内的电价、负载和光伏预测功率；
- 决定每个 10 分钟时段的计划购电量、储能充电量和放电量；
- 满足负载、储能容量、充放电功率和日初日末 SOC 相同等约束；
- 目标是最小化全天计划购电费用；
- 输出 `result1.xlsx`。

### 问题 2：逐日计划与紧急购电

- 负载和光伏发电随日期变化；
- 每天 0:00 制定当天计划；
- 实际供电不足时，以当时电价 5 倍进行紧急购电；
- 计划购电费用仍然按计划量结算，不按实际使用量结算；
- 需要处理预测偏差、跨日 SOC 和信息不可提前获得的问题；
- 输出 `result2.xlsx`。

### 问题 3：多阶段滚动 MPC

- 每天 0:00、6:00、12:00、18:00 获得新的未来 24 小时光伏预测；
- 0:00 制定计划购电策略；
- 6:00、12:00、18:00 只调整尚未执行的未来时段；
- 已经过去的决策必须固定，不能重新计算；
- 区分计划购电、调整购电和紧急购电三类变量及费用；
- 比较是否有必要使用后续时刻的预测；
- 输出 `result3.xlsx`。

### 问题 4：波动电价扩展

- 保留问题 2 和问题 3 的模型结构；
- 用附件 4 的逐时波动电价替换固定日内电价；
- 重新计算逐日策略和滚动 MPC 策略；
- 输出 `result4-2.xlsx` 和 `result4-3.xlsx`。

## 3. 建议的项目目录

```text
C题项目/
├── README.md
├── 题目/                         # 原始题目和附件，只读，不修改
│   ├── C题.pdf
│   └── 附件/
│       ├── 附件1.xlsx
│       ├── 附件2.xlsx
│       ├── 附件3.xlsx
│       ├── 附件4.xlsx
│       └── 附件5/
├── 问题1/
│   ├── 建模笔记.md
│   ├── 代码/
│   └── 结果/
├── 问题2/
│   ├── 建模笔记.md
│   ├── 代码/
│   └── 结果/
├── 问题3/
│   ├── 建模笔记.md
│   ├── 代码/
│   └── 结果/
├── 问题4/
│   ├── 建模笔记.md
│   ├── 代码/
│   └── 结果/
├── 公共代码/                     # 数据读取、时间转换、SOC、结果校验
├── 论文/
│   ├── 论文.md
│   ├── 论文.tex
│   ├── 论文.pdf
│   └── 附图/
└── 参考资料/
```

注意：

- `题目/`中的原始文件不得修改；
- 代码生成的文件放进对应问题的`结果/`；
- 每个数值结果都要能追溯到代码和输出文件；
- 不要把题目、代码或讨论内容放到公开 GitHub 仓库；
- 如果使用版本管理，只使用队内私有仓库或本地 Git，并遵守竞赛纪律。

## 4. 最简单的环境配置

### 4.1 需要安装的软件

所有队员：

1. Python 3.10 或更高版本，推荐 Python 3.11；
2. VS Code 或 PyCharm；
3. Excel、WPS 或 LibreOffice，用于查看结果模板。

负责论文的队员还需要：

4. TeX Live、MacTeX 或 MiKTeX，确保可以运行 XeLaTeX；
5. VS Code 的 LaTeX Workshop 插件，可选。

### 4.2 创建 Python 虚拟环境

进入项目根目录后执行。

Windows PowerShell：

```powershell
py -3.11 -m venv .venv
.\.venv\Scripts\Activate.ps1
```

macOS 或 Linux：

```bash
python3 -m venv .venv
source .venv/bin/activate
```

激活成功后，命令行开头通常会出现 `(.venv)`。

### 4.3 安装 Python 依赖

```bash
python -m pip install --upgrade pip
python -m pip install numpy pandas scipy openpyxl matplotlib seaborn scikit-learn
```

这些包的用途：

| 包 | 用途 |
|---|---|
| `numpy` | 数值计算 |
| `pandas` | 读取、整理和检查表格数据 |
| `scipy` | 使用内置 HiGHS 求解 MILP |
| `openpyxl` | 读取和填写题目给出的 Excel 模板 |
| `matplotlib`、`seaborn` | 绘制负载、光伏、SOC和成本图 |
| `scikit-learn` | 问题 2 的负载或光伏预测、误差评价 |

本项目建议先使用 `scipy.optimize.milp`，不需要配置商业求解器。模型规模过大或求解速度不足时，再考虑 Gurobi；不要一开始就增加环境复杂度。

### 4.4 检查环境

```bash
python -c "import numpy, pandas, scipy, openpyxl, matplotlib, sklearn; from scipy.optimize import milp; print('Python 环境正常')"
```

论文成员再检查：

```bash
xelatex --version
```

如果两条命令都能正常输出，环境配置完成。

## 5. 开始建模前必须统一的口径

### 5.1 时间粒度

每天有 144 个时间位置，间隔为 10 分钟：

```text
00:10, 00:20, ..., 23:50, 0:00+1
```

不要自行增加一个额外的 `00:00` 数据点，否则会产生 145 个位置并造成整体错位。

推荐在程序内部使用：

```python
slot = 0, 1, ..., 143
```

附件的第 `slot` 列与结果模板的第 `slot` 个购电时段按位置一一对应。程序内部使用整数下标，最后写结果时再使用模板原有表头。

附件 5 的部分模板最后一列表头写成 `0:00-0:10+1`。内部应把它看作最后一个跨日时间位置，但写回时不要修改模板表头。

### 5.2 功率与电量

附件中的负载和光伏数据单位是 kW，模型决策变量和结果文件要求的是 kWh。

```text
10 分钟 = 1/6 小时
电量 kWh = 功率 kW × 1/6 小时
```

即：

```math
E_t^{load}=P_t^{load}/6,
\qquad
E_t^{pv}=P_t^{pv}/6.
```

最大充放电功率为 5000 kW，因此每个 10 分钟时段的最大充电量或放电量为：

```math
5000/6=833.333\ \text{kWh}.
```

### 5.3 电量平衡

供电量必须不低于负载，因此使用不等式：

```math
q_t^{grid}+q_t^{emergency}+E_t^{pv}+D_t
\ge E_t^{load}+C_t.
```

不要直接写成无弃电量的等号。计划购电或光伏多余时，可以存在未利用电量。

### 5.4 储能 SOC

统一采用下列变量口径：`C_t`和`D_t`表示储能外部接口处的充电量和放电量。

```math
S_{t+1}=S_t+0.9C_t-\frac{D_t}{0.9}.
```

约束为：

```math
1200\le S_t\le10800.
```

跨日时必须满足：

```math
S_{d,144}=S_{d+1,0}.
```

不能每天都把 SOC 重新设置成 6000 kWh。6000 kWh 只对应题目给出的初始时刻，除非某一问题明确要求日初日末相同。

### 5.5 三类购电必须分开

建议使用不同变量：

```text
q_plan       0:00 制定的计划购电量
q_adjust     获得新预测后调整的购电量
q_emergency  实际供电不足时的紧急购电量
```

不能用一个“电网购电量”变量同时代表三种购电，因为三者的信息时间和价格规则不同。

计划购电费始终按照计划量计算：

```math
C^{plan}=\sum_t \pi_t q_t^{plan}.
```

不能按照计划电中实际被负载或储能使用的部分计算。

紧急购电费为：

```math
C^{emergency}=\sum_t5\pi_tq_t^{emergency}.
```

问题 3 还需要分别计算增加调整量和减少调整量对应的费用。

### 5.6 信息集

模型在某个时刻只能使用当时已经获得的信息。

- 每天 0:00 不能使用当天未来实际负载或实际光伏；
- 问题 2 可以使用此前日期的历史数据建立预测；
- 1 月数据可以用于预测训练、参数初始化和 SOC 热启动；
- 结果从 2 月 1 日开始输出；
- 6:00 发布的新预测只能影响 6:00 之后的决策；
- 12:00 和 18:00 同理；
- 已执行的变量必须锁定，不能回头修改。

### 5.7 附件 3 的时间映射

`预报 h 小时`表示“发布时间加 h 小时”，不是当天固定的第 h 个整点。

例如：

```text
6:00 发布的预报 1 小时 → 当天 7:00
18:00 发布的预报 7 小时 → 次日 1:00
```

附件 3 是整点小时预测，而决策时间粒度是 10 分钟。必须在`建模笔记.md`中明确小时预测转换为 10 分钟预测的方法，例如分段线性插值，并保证跨日时间正确。

## 6. 推荐的代码顺序

以下文件名只是建议，队友可以按这个顺序工作：

```text
公共代码/
├── data_loader.py          # 读取附件并统一时间索引
├── time_utils.py           # 144 时段和跨日转换
├── storage.py              # SOC 和充放电公共约束
├── validation.py           # 单位、平衡、SOC和成本检查
└── export_excel.py         # 写入原始结果模板

问题1/代码/
└── solve_q1.py

问题2/代码/
├── forecast_q2.py
└── solve_q2.py

问题3/代码/
└── solve_q3_mpc.py

问题4/代码/
├── solve_q4_2.py
└── solve_q4_3.py
```

推荐实际执行顺序：

1. 读取附件，但不改原文件；
2. 检查所有日期、时间列、单位和缺失值；
3. 建立问题 1 的基础 MILP；
4. 独立验证问题 1 的供电、SOC和费用；
5. 将问题 1 扩展为问题 2 的逐日模型；
6. 建立问题 2 所需的预测方法，禁止未来数据泄漏；
7. 在问题 2 基础上增加问题 3 的滚动 MPC；
8. 替换电价数据，完成问题 4；
9. 运行统一校验程序；
10. 将结果写入五个原始 Excel 模板的副本。

## 7. 每一问的建模笔记怎么写

每个`建模笔记.md`统一使用以下结构：

```markdown
# 问题 X 建模笔记

## 1. 问题目标
## 2. 当前时刻可用信息
## 3. 数据与时间范围
## 4. 参数和变量
## 5. 模型假设
## 6. 目标函数
## 7. 约束条件
## 8. 求解方法和参数
## 9. 结果文件
## 10. 可行性与正确性检查
## 11. 本问结论
## 12. 与下一问的关系
```

每一问都必须形成：

```text
问题分析 → 模型建立 → 模型求解 → 结果分析 → 本问小结
```

## 8. 最低限度的结果校验

每次运行后至少检查：

- [ ] 每天正好有 144 个时间位置；
- [ ] 功率已经乘以 `1/6` 转换为电量；
- [ ] 供电量在所有时段均不低于负载；
- [ ] SOC 始终位于 1200—10800 kWh；
- [ ] 单时段充放电量不超过 833.333 kWh；
- [ ] 没有无意义的同时充电和放电；
- [ ] 相邻日期的 SOC 连续；
- [ ] 计划费用按计划量计算；
- [ ] 紧急购电按 5 倍电价计算；
- [ ] MPC 没有修改过去的决策；
- [ ] 目标函数值与独立复算费用一致；
- [ ] 输出日期、工作表名称、行列数量与模板一致；
- [ ] 没有覆盖题目提供的原始模板。

## 9. 团队最简单的分工

### 建模队员

- 统一变量、目标函数和约束；
- 维护各问的`建模笔记.md`；
- 重点检查信息集和跨日 SOC。

### 编程队员

- 编写数据读取、MILP、MPC和结果导出代码；
- 每次运行后保存结果和日志；
- 维护统一的校验程序。

### 论文队员

- 根据建模笔记和真实结果撰写论文；
- 统一符号、公式、表格和图片；
- 最后写摘要；
- 使用 XeLaTeX 生成 PDF。

分工不等于完全分离。每天至少进行一次三方核对：模型公式、代码变量和论文符号必须一致。

## 10. 如何使用 cumcm-paper-hand-skill

在 Codex 中打开整个`C题项目/`文件夹，然后分阶段发送指令。

第一次扫描：

```text
请使用 cumcm-paper-hand-skill 扫描当前参赛文件夹，读取题目和附件，列出四个问题、数据文件、现有代码、结果和缺失材料。暂时不要重新建模。
```

明确要求建立模型：

```text
请使用 cumcm-paper-hand-skill 重新建立问题1的模型并求解。必须遵守项目中的建模笔记、时间索引、功率到电量转换、电量平衡不等式和计划购电结算规则。
```

已有代码后进行核对：

```text
请核对问题2的建模笔记、代码和结果，不要重新建模。重点检查未来数据泄漏、跨日SOC、紧急购电费用和结果模板格式。
```

准备写论文：

```text
请读取四个问题的建模笔记和结果，先梳理问题1到问题4的递进关系，生成Markdown论文草稿。所有数值必须来自结果文件，不得编造。
```

草稿确认后：

```text
请将确认后的论文草稿整理成LaTeX，使用XeLaTeX编译两次生成PDF，并执行最终检查清单。
```

注意：这个 skill 默认会直接采用已经存在的模型、代码和结果。只有明确写出“重新建模”或“重新求解”，它才会执行完整的建模求解流程。

## 11. 最终交付物

```text
论文/论文.tex
论文/论文.pdf
result1.xlsx
result2.xlsx
result3.xlsx
result4-2.xlsx
result4-3.xlsx
各问题的可运行源代码
必要的图表、模型结果和说明文件
```

最终原则：先统一时间、单位和信息集，再写模型；先验证模型和结果，再写论文；论文中的每个数值都必须能从代码和结果文件中重新得到。
